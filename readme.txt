for install
    1. run setup.sh (script setup.sh)
to run program
    1. make sure you are in correct directory (cd portsetter)
    2. make portsetter
    3. make test
    4. setport [flag] [port_number]
    5. for help use flag -h
    
to change setting in bash to spanish
    1. export LANGUAGE=en_US
        i. that is the default if you want to change other options change var 
        
program use
    1. "setport -h" "setport --help" "setport -?" or "setport" will explain the usage of
            the program.
    